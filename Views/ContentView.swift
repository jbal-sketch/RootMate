//
//  ContentView.swift
//  RootMate
//
//  Created on $(date)
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, RootMate!")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}

